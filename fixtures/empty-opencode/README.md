# Empty OpenCode Fixture

This fixture represents a repository with no application stack and no OpenCode configuration yet.

It should only trigger baseline setup recommendations such as a project `opencode.jsonc` starter and `AGENTS.md`.
